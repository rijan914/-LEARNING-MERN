let name = `rijan`
let surname= `bhandari`

let fullname = `my name is ${name} and surname is ${surname}`
console.log(fullname)


// we can call variable inside in  baptics but we cannot call variable in single and double quote.
